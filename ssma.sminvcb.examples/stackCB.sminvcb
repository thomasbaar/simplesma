project stackCB refers_to stack

adapter_class "blabla"
						
import stack.*;
  
code_vars c_s
state_preds
//	nonEmpty: 6;  //not bool
//	nonEmpty: num > 0;  // not only code-vars
	nonEmpty: c_s > 0;
	isEmpty : c_s == 0; 
	      
global_preds	     
	def_num: num  == c_s;    
	
	